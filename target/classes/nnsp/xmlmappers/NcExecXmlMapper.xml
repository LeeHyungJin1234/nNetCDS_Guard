<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="nnsp.xmlmappers.NcExecXmlMapper">

	<select id="select_exec" parameterType="map" resultType="java.util.HashMap">
		${sql_exec}
	</select>
	
	<insert id="insert_exec" parameterType="map" >
        ${sql_exec}
    </insert>
    
    <update id="update_exec" parameterType="map" >
        ${sql_exec}
    </update>
	
</mapper>